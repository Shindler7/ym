# YM — Консольный менеджер для общения с нейросетями

[![Rust](https://img.shields.io/badge/rust-1.75%2B-orange?logo=rust)](https://www.rust-lang.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

Терминальный клиент для текстового взаимодействия с YandexGPT, написанный на Rust. Проект предоставляет минималистичный TUI (Text-based User Interface) для диалога с нейросетью прямо из командной строки.

## ✨ Возможности

*   **Полноценный TUI-интерфейс**: Удобное управление через клавиатуру с подсветкой курсора, историей сообщений и скроллингом.
*   **Интеграция с YandexGPT**: Поддержка моделей `yandexgpt/latest` и других через официальное API.
*   **Безопасное хранение ключей**: Данные аутентификации (`API-Key`, `ID-Catalog`) хранятся в зашифрованном виде в конфигурационных директориях ОС.
*   **Простая настройка**: Инициализация доступна через единственную команду `ym --init`.
*   **Гибкая конфигурация**: Возможность настройки параметров генерации (температура, количество токенов) в коде.
*   **Обработка ошибок**: Информативные сообщения об ошибках сети и API.

<img width="1774" height="1627" alt="ym screen" src="https://github.com/user-attachments/assets/88665164-dde3-4b0d-8485-b1ae6644ccf6" />

## 🚀 Быстрый старт

### Предварительные требования

1.  **Rust Toolchain**: Установите Rust и Cargo (версия 1.75 или выше). [Инструкция](https://www.rust-lang.org/tools/install).
2.  **Yandex Cloud API Key**: Вам понадобятся:
    *   `API-Key` для сервиса `YandexGPT`
    *   `ID-Catalog` (идентификатор каталога)
    *   Получить их можно в [Yandex Cloud Console](https://console.cloud.yandex.ru/).

### Установка и запуск

1.  **Клонируйте репозиторий и соберите проект**:
    ```bash
    git clone git@github.com:Shindler7/ym.git
    cd ym
    cargo build --release
    ```
    Исполняемый файл появится в `./target/release/ym`.

2.  **Первоначальная настройка**:
    ```bash
    # Запустите инициализацию для ввода учетных данных
    ./target/release/ym --init
    ```
    Следуйте инструкциям в терминале. Данные сохранятся локально. 
    **Важно**: ваши чувствительные данные не шифруются, хранятся в файле
    открыто.

3.  **Запустите чат**:
    ```bash
    ./target/release/ym
    ```

### Управление в интерфейсе

| Действие                    | Сочетание клавиш                |
|-----------------------------|----------------------------------|
| Отправить сообщение         | `Enter`                          |
| Перемещение курсора         | Стрелки `←` `→`, `Home`, `End`  |
| Перемещение по словам       | `Ctrl + ←` / `Ctrl + →`         |
| Удаление символов           | `Backspace`, `Delete`           |
| **Выход из приложения**     | `Esc` или `Ctrl + C`            |

**Ключевые зависимости**:
*   `ratatui` + `crossterm` — для построения кросс-платформенного TUI.
*   `tokio` — асинхронный рантайм.
*   `reqwest` — HTTP-клиент для запросов к API.
*   `clap` — парсинг аргументов командной строки.
*   `serde` — сериализация/десериализация данных.

## 🔧 Разработка и расширение

Проект задуман как учебный и открыт для доработок. Вот несколько идей для будущих улучшений:

1.  **Поддержка других моделей/провайдеров** (OpenAI, Anthropic, локальные LLM).
2.  **Режим чата с историей** (сохранение контекста между запросами).
3.  **Streaming-ответы** — вывод ответа по мере генерации.
4.  **Конфигурационный файл** (TOML/YAML) для настройки параметров без пересборки.
5.  **Подсветка синтаксиса** в сообщениях с кодом.
6.  **Логирование** сессий в файл (Markdown, JSON).

### Генерация документации

Вся документация по модулям и структурам доступна через `cargo doc`:
```bash
cargo doc --open --document-private-items --no-deps
```

### История версий

*0.1.1* (07.12.2025) — исправлены некорректные декларации типов в `settings.rs`.

*0.1.0* (07.12.2025) — задумка, работающая концепция.

## License

This project is licensed under the MIT license ([LICENSE] or <http://opensource.org/licenses/MIT>)

[LICENSE]: ./LICENSE
